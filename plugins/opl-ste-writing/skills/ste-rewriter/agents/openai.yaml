interface:
  display_name: "STE Rewriter"
  short_description: "Rewrites ambiguous English into ASD-STE100 style — one meaning per word, active voice, simple tense, short sentences, significantly reducing `AI slop`."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Lean Delivery"
  short_description: "Ship substantial changes with bounded review"
  default_prompt: "Use $lean-delivery to deliver this substantial change with compact context and bounded review."

policy:
  allow_implicit_invocation: true

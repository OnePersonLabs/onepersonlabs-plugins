interface:
  display_name: "Long Command Wakeup"
  short_description: "Queue continuation after long commands finish"
  default_prompt: "Use $long-command-wakeup for this known long-running unattended command."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Debug"
  short_description: "Find causal bugs and verify lasting fixes"
  default_prompt: "Use $debug to investigate this failure and, when requested, implement and verify a fix."
policy:
  allow_implicit_invocation: true

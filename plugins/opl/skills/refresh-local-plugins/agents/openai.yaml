interface:
  display_name: "Refresh Local Plugins"
  short_description: "Refresh selected local Codex plugins"
  default_prompt: "Use $refresh-local-plugins to refresh the selected plugins from this checkout into the authorized Codex home."
policy:
  allow_implicit_invocation: true

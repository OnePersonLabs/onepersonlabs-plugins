interface:
  display_name: "Verification Before Completion"
  short_description: "Require fresh evidence before success claims"
  default_prompt: "Use $verification-before-completion to verify this work before declaring it complete."

policy:
  allow_implicit_invocation: true

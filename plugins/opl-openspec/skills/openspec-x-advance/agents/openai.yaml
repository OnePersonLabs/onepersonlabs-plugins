interface:
  display_name: "OpenSpec X Advance"
  short_description: "Select and finish the next unblocked OpenSpec change"
  default_prompt: "Use $openspec-x-advance to drive the next unblocked OpenSpec change to archive."

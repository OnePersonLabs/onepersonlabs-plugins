interface:
  display_name: "OpenSpec X Reverse Apply"
  short_description: "Update tasks in an OpenSpec change, based on actual implementation state (completed, stale, pending)."
policy:
  allow_implicit_invocation: false

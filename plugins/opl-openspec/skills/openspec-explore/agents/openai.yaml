interface:
  display_name: "OpenSpec Explore"
  short_description: "Explore an idea before committing to an OpenSpec change"
  default_prompt: "Use $openspec-explore to investigate and clarify this idea before implementation."

policy:
  allow_implicit_invocation: true

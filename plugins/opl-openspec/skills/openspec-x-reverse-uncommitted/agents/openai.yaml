interface:
  display_name: "OpenSpec X Reverse Uncommitted"
  short_description: "Create a clean OpenSpec proposal from dirty worktree drift"
  default_prompt: "Use $openspec-x-reverse-uncommitted to inspect staged and unstaged changes, reject ghost deltas, audit stale residue, and propose a reverse-engineered OpenSpec change."
